# Changelog

## 0.1.0

Initial release. Bundles woku's MCP server connection (`https://api.woku.app/mcp`) and the woku skill (the woku method plus the `woku_guide` / `design_voc_program` entry points). Per-client manifests for Claude Code, Codex, and OpenClaw/OpenCode.
